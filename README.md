# AgentScan Documentation

面向 AgentScan 用户的独立文档站。当前仓库只保存文档网站，不配置远程仓库。

## 本地开发

```bash
npm install
npm run dev
```

工程事实以 AgentScan 主仓库的 `docs/REQUIREMENTS_STATUS.md`、`docs/ARCHITECTURE.md` 和 `docs/PRIVACY.md` 为准。文档发布前需要根据对应版本重新核对功能状态。
